{
  "name": "@kumix/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Private MCP server for @kumix/better-auth-ui: package metadata, component search, source, and per-file import examples",
  "author": "Kumix Labs <hai@kumix.io>",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "kumix-mcp-better-auth-ui": "dist/index.js"
  },
  "scripts": {
    "clean": "rimraf dist",
    "clean:all": "rimraf .turbo dist node_modules",
    "build": "tsc",
    "dev": "bun src/index.ts",
    "start": "node dist/index.js",
    "types:check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "glob": "^13.0.6",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@kumix/tsconfig": "^0.1.0",
    "@types/node": "^26.6.2"
  }
}
